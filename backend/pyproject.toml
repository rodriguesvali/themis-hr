[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "themis-hr-api"
version = "0.1.0"
description = "FastAPI backend and CrewAI orchestration for Themis HR."
requires-python = ">=3.12"

[tool.setuptools.packages.find]
where = ["src"]
